const propPassing = ({ title, price, img }) => {
  const items = [
    {
      id: "1",
      product: title,
      image: img,
      price: price,
    },
 
  ];
  return items;
};


export default propPassing;
